

const RenderedResume = ({}) => {
  //TODO: 
  // 1. get the selected template
  // 1.1. based on the template type render a wrapper with side section or without
  // 1.2. based on the selected template render the primary color for the side section

  return (<div className="RenderedResume">Resume</div>)
};

export default RenderedResume;
